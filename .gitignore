# Variáveis de ambiente (API keys, tokens — NUNCA commitar)
.env
.env.local
.env.*

# Credenciais Google (OAuth client, tokens de service account — NUNCA commitar)
credenciais/

# Arquivo gerado pelo /setup com MCPs específicos do usuário
_configurar-mcps.md

# Outputs de renderização (binários pesados)
*.png
!marca/*.png

# Arquivos de dados do usuário (CSVs, planilhas, etc)
dados/*.csv
dados/*.xlsx
dados/*.xls
dados/*.txt
dados/*.pdf
dados/*.json
!dados/README.md

# Pastas de trabalho do usuário (geradas pelo /setup, conteúdo pessoal)
clientes/
propostas/
conteudo/
projetos/
briefings/
trabalho/
estudos/
publicacoes/
anotacoes/
curriculo/
tarefas.md


# Ponte pro Codex enxergar as skills — criada pelo /setup em CADA máquina
# (symlink no Mac/Linux, cópia no Windows). Nunca vai pro git.
.agents/

# Arquivos gerados pelo uso/registro das skills (conteúdo pessoal, nunca no kit)
automacoes-dm.md
prioridades.md
contas.yaml

# Sistema
.DS_Store
node_modules/

# cache do Python
__pycache__/
*.pyc

# banco local de questoes do Tec: fica no Drive, nunca no repositorio
dados/banco-tec/
